---
title: 功能文档模板
description: 功能文档的标准结构和内容要求。使用 `/understand-feature` 命令创建新功能文档时，按此模板产出。
---

# [功能名称] — 功能文档

> 本文档是该功能的完整参考，包含所有修改、编辑或扩展该功能需要了解的内容。

## 功能概述

简要说明该功能解决的业务问题、核心职责和定位。一句话总结其在整个项目中的位置。

## 文件清单

### 后端

| 文件 | 路径 | 用途 |
|------|------|------|
| 入口 | `backend/app/...` | 简要说明 |
| 服务 | `backend/app/...` | 简要说明 |
| 数据 | `backend/app/...` | 简要说明 |
| 测试 | `backend/tests/...` | 简要说明 |

### 前端

| 文件 | 路径 | 用途 |
|------|------|------|
| 页面 | `frontend/src/pages/...` | 简要说明 |
| 组件 | `frontend/src/components/...` | 简要说明 |
| API | `frontend/src/lib/...` | 简要说明 |
| 测试 | `frontend/src/...` | 简要说明 |

### 配置/数据

| 文件 | 路径 | 用途 |
|------|------|------|
| 配置 | `...` | 简要说明 |
| 数据 | `...` | 简要说明 |
| 文档 | `...` | 简要说明 |

## 业务逻辑

### 核心流程

描述该功能的核心业务逻辑，包含关键步骤和决策点。

```text
[步骤1] → [步骤2] → [分支判断] → [步骤3a] / [步骤3b] → [最终输出]
```

### 数据流

描述数据在该功能中如何流转：输入 → 处理 → 输出。

1. **输入来源**：数据从哪里来（API 请求、定时任务、数据源同步、用户操作等）
2. **处理过程**：数据经过哪些转换、计算、过滤、聚合
3. **输出去向**：结果写入哪里、通过什么方式提供给消费者

### 调用链

从入口到最终输出的完整调用链，标注关键 `文件:行号`。

```text
入口（文件:行号）→ 服务层（文件:行号）→ 仓库/Provider（文件:行号）→ 指标/策略（文件:行号）→ 输出（文件:行号）
```

### 状态机（如适用）

描述该功能涉及的状态及其转换关系。

## 关键数据结构

### API 契约

请求/响应结构（关键字段说明）。

### 存储结构

Parquet schema、JSON 文件结构、缓存键格式等。

### 内存结构

服务内部使用的缓存、状态、队列等数据结构。

## 扩展与修改指南

### L1 扩展（配置/策略文件/扩展数据）

在该功能层级上，通过配置或策略文件可实现哪些定制。

### L2 扩展（插槽/路由/注册替换）

该功能提供了哪些扩展点或插槽。

### L3 修改（直接改源码）

如果必须修改核心源码，建议的最小改动方式和注意事项。

### 缓存失效影响

该功能的写操作涉及哪些缓存层，修改后需要清理哪些缓存。

| 缓存层 | 失效方式 | 影响范围 |
|--------|----------|----------|
| 文件缓存 | 说明 | 影响 |
| 内存缓存 | 说明 | 影响 |
| SSE/前端 | 说明 | 影响 |

### 测试

| 测试类型 | 位置 | 关键测试用例 |
|----------|------|-------------|
| 单元测试 | `backend/tests/...` | 正常/边界/失败路径 |
| 集成测试 | `backend/tests/...` | 数据流/缓存/SSe |
| 前端测试 | `frontend/src/...` | 渲染/交互/状态 |

## 依赖关系

### 依赖的其他功能

- [功能名称](链接)：依赖说明

### 被依赖的功能

- [功能名称](链接)：被依赖说明

## 常见问题与注意事项

- 已知的陷阱和限制
- 容易出错的地方
- 性能注意事项
- 安全注意事项